'use babel'

import ReactComponentPreviewView from '../lib/react-component-preview-view'

describe('ReactComponentPreviewView', () => {
	it('has one valid test', () => {
		expect('life').toBe('easy')
	})
})
